# 微信聊天记录导出 · 使用说明

## 怎么用（最简单）
**双击 `启动.command`** 就行。

- **第一次**会自动：装好依赖 → 让你给微信签名一次（要输开机密码）→ 提取密钥 → 打开界面。
  - 开始前请：打开并登录微信，随便打开一个**有图片**的聊天，往上滚动让图片显示出来。
  - 中途会提示你「退出微信再重开」，照做即可。
- **以后**再双击，直接打开界面（不用再签名/输密码）。

界面打开后：选联系人 → 「预览/加载消息」→（可点 ✕ 删除不想要的）→ 勾选格式 → 「导出」。
导出的文件默认存到**桌面**。

> 如果双击没反应：右键 `启动.command` →「打开」，在弹窗里再点「打开」（macOS 首次会拦截未签名脚本）。
> 或者打开「终端」，把 `启动.command` 拖进去再回车。

## 导出格式
- **HTML**（推荐）：网页版，还原微信气泡。从界面导出的是**自包含单文件**，图片/头像/语音/视频都在文件里，发给别人也能直接打开。
- **PDF / A4图片**：用于**打印**，A4 纸排版，`紧凑`密度每页塞更多、打印页数最少。
- **CSV**：表格（可用 Excel 打开）。
- **TXT**：纯文本。

## 换台 Mac 用
把**整个文件夹**拷过去（`.venv` 可不拷），双击 `启动.command` 即可——它会在新机器上自动重建环境、重新签名、提取该机器的密钥。

## 说明 / 注意
- 仅适用于 **macOS**、且导出**你自己**登录的微信（4.0+）。全程只读，不改动微信数据。
- 语音转文字、图片解码都在**本机离线**完成，不上传任何数据。
- 若哪天解密失败（多为微信升级后密钥变化）：删掉 `~/.wechat_exporter/keys.json`，重开微信后再双击 `启动.command` 重新提取。
- 仅供个人备份自己的聊天记录使用。

## 命令行（进阶，可不看）
```
.venv/bin/python main.py --help              # 全部参数
.venv/bin/python main.py --contact 张三 --format all --voice --self-contained --yes
```
