# 微信聊天记录导出 · 简明使用说明

1. 打开 `微信聊天记录导出.app`。首次被 macOS 拦截时，在 Finder 中右键应用选择“打开”。
2. 在“系统设置 > 隐私与安全性 > 完全磁盘访问权限”中允许本应用，然后重开应用。
3. 点击页面顶部“首次设置”。当前微信会退出，工具将自动打开一个用户目录中的临时签名
   副本；原始 `/Applications/WeChat.app` 不会被修改。
4. 设置成功后选择“好友”或“群聊”，也可以按昵称、备注或微信号搜索。
5. 选择日期与导出格式，点击“预览 / 加载消息”，确认后点击“导出”。文件默认在桌面。
6. 导出目录还会生成 `*_归档说明.txt` 和 `*_归档校验.json`。A4 PNG 文件名及
   PNG/PDF 页脚均标明“第 X 页 / 共 Y 页”，每条消息带有连续顺序号。
7. HTML、TXT、PNG 和 PDF 中的每条消息均显示完整年月日与时分秒；JSON 和 CSV 还保存
   ISO 8601、UTC 偏移及 Unix 时间，便于跨时区核验。

归档清单记录会话名称、昵称/备注、微信号、账号标识、消息范围、消息哈希链和导出文件的
SHA-256，用于辅助核验导出后的文件是否发生变化。它不等同于电子签名、可信时间戳、公证
或司法鉴定；用于正式程序时请同时保留原设备、原始数据库、完整导出目录和操作过程记录。

本工具仅在本机运行，只适用于备份本人有权访问的数据。密钥和明文数据库缓存位于
`~/.wechat_exporter/`，属于敏感文件。完整说明、故障排查与免责声明请阅读 `README.md`。
